package com.api.ppp.back.daos;

import com.api.ppp.back.models.Aspecto;
import org.springframework.stereotype.Repository;

@Repository
public interface AspectoRepository extends BaseRepository<Aspecto, Integer> {
}
